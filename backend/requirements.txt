# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
websockets==14.1

# --- Data / ORM ---
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# --- Validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Security ---
# bcrypt pinned to 4.0.1: passlib 1.7.4 reads bcrypt.__about__ which was
# removed in bcrypt >= 4.1, producing a harmless but noisy startup warning.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyjwt==2.10.1

# --- Object storage (optional, for MinIO/S3 backend) ---
boto3==1.35.90

# --- Testing ---
pytest==8.3.4
httpx==0.28.1
